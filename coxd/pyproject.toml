[project]
name = "coxd"
version = "0.0.1"
description = "Coxswain v3.5 supervisor — control plane on the Claude Agent SDK (DESIGN-V35)"
requires-python = ">=3.12"
dependencies = [
    "claude-agent-sdk>=0.2.119",
]

[tool.uv]
# codex-sdk (reviewer lane, D22) added when the review lane is built.
